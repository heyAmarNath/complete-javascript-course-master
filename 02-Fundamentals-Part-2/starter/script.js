'use strict';
/*
function logger() {
    console.log('My name is Amar Nath');
}
logger();
//this function does not return anything

function juice(apples, oranges) {
    const juice = `Juice🧃 with ${apples} apples and ${oranges} oranges`;
    return juice;
}
console.log(juice(3, 4));
//this function is taking something and returning something

//FUNCTION DECLARATION
function calcAge1(birthYear) {
    return 2021 - birthYear;
}
console.log(calcAge1(1997));

//FUNCTION EXPRESSION
const calcAge2 = function (birthYear) {
    return 2021 - birthYear;
}
console.log(calcAge2(1997));

//ARROW FUNCTION
const calcAge3 = birthYear => 2021 - birthYear;
console.log(calcAge3(1997));

const retirementAge = (firstName, birthYear) => {
    const age = 2021 - birthYear;
    const retirement = 65 - age;
    return `${firstName} will retire in ${retirement} years`;
}
console.log(retirementAge('Amar', 1997));

//FUNCTION CALLING ANOTHER FUNCTION
const peices = function (fruit) {
    return 4 * fruit;
}
const juiceMachine = function (apples, oranges) {
    const applePeices = peices(apples);
    const orangesPeices = peices(oranges);
    return `Juice with ${applePeices} peices of apple and ${orangesPeices} peices of oranges.`;
}
console.log(juiceMachine(3,4));

//ARRAYS
const letters = ['alpha', 'beta', 'gamma', 'delta'];
console.log(letters);

//METHOD IN AN ARRAY
letters.push('omicron');
letters.unshift('zero');
console.log(letters.pop());
letters.shift();
console.log(letters);

console.log(letters.indexOf('beta'));
console.log(letters.includes('gamma'));

//OBJECTS
const amar={
    firstName:'Amar',
    lastName:'Nath',
    birthYear:1999,
    calcAge: function () {
        return 2021 - this.birthYear;
    },
    
};

console.log(amar.firstName);
console.log(amar['firstName']);
console.log(amar.calcAge());

*/

